import { PeliculaimagenPipe } from './peliculaimagen.pipe';

describe('PeliculaimagenPipe', () => {
  it('create an instance', () => {
    const pipe = new PeliculaimagenPipe();
    expect(pipe).toBeTruthy();
  });
});
